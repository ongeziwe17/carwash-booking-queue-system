services:
  carwash-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: carwash-booking-queue-system:local
    env_file:
      - .env
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
