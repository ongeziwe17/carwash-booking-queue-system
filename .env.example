SPRING_PROFILES_ACTIVE=default
SERVER_PORT=8080
# Generate a local value with: openssl rand -base64 32
# This deliberately invalid placeholder must be replaced in .env.
CARWASH_SECURITY_JWT_SECRET=REPLACE_WITH_BASE64_ENCODED_32_BYTE_SECRET