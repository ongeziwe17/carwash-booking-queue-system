info: { name: Confirm second branch booking, type: http, seq: 6, tags: [ops, booking, workflow] }
http:
  method: POST
  url: "{{baseUrl}}/api/bookings/{{branchOpsSecondBookingId}}/confirm"
  auth: { type: bearer, token: "{{adminToken}}" }
runtime:
  scripts:
    - type: tests
      code: test("returns CONFIRMED", function() { expect(res.status).to.equal(200); expect(res.body.status).to.equal("CONFIRMED"); });
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
