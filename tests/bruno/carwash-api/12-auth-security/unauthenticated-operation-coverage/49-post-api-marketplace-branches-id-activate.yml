info: { name: "401 POST /api/marketplace/branches/{id}/activate", type: http, seq: 49, tags: [auth, security, marketplace] }
http: { method: POST, url: "{{baseUrl}}/api/marketplace/branches/{{marketplaceBranchId}}/activate", auth: { type: none } }
runtime:
  scripts:
    - type: tests
      code: 'test("returns standard 401", function() { expect(res.status).to.equal(401); expect(res.body.code).to.equal("AUTHENTICATION_REQUIRED"); });'
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
