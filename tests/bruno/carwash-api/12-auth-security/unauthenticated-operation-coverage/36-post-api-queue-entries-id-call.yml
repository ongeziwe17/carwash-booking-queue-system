info:
  name: 401 POST /api/queue-entries/{id}/call
  type: http
  seq: 36
  tags:
    - auth
    - security

http:
  method: POST
  url: "{{baseUrl}}/api/queue-entries/{{queueEntryId}}/call"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns HTTP 401", function() { expect(res.status).to.equal(401); });
        test("uses AUTHENTICATION_REQUIRED", function() { expect(res.body.code).to.equal("AUTHENTICATION_REQUIRED"); expect(res.body.status).to.equal(401); });
        test("reports request path", function() { expect(res.body.path).to.equal(String(req.getUrl()).replace(/^https?:\/\/[^/]+/, "").split("?")[0]); });
        test("has ISO timestamp and fieldErrors", function() { expect(Number.isNaN(Date.parse(res.body.timestamp))).to.equal(false); expect(Array.isArray(res.body.fieldErrors)).to.equal(true); });
        test("uses safe message", function() { expect(res.body.message).to.equal("Authentication is required"); });
        const payload = JSON.stringify(res.body ?? {});
        const lower = payload.toLowerCase();
        for (const forbidden of ["encodedpassword", "passwordhash", "stacktrace", "java.lang", "authorization", "secure_jwt_secret", "bootstrap-admin-password"]) {
          expect(lower, `response leaked ${forbidden}`).to.not.include(forbidden);
        }
        test("does not expose access tokens", function() { expect(lower).to.not.include("accesstoken"); });

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: false
