info:
  name: Server appends second queue entry
  type: http
  seq: 23
  tags:
    - workflow
    - integrity

http:
  method: POST
  url: "{{baseUrl}}/api/queue-entries"
  headers:
    - name: Content-Type
      value: "application/json"
  body:
    type: json
    data: |-
      {
        "queueEntryId": "{{queueOrderingEntry2Id}}",
        "bookingId": "{{queueOrderingBooking2Id}}",
        "serviceId": "{{queueOrderingService25Id}}"
      }
  auth:
    type: bearer
    token: "{{staffToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 201", function() { expect(res.status).to.equal(201); });
        test("second entry uses first duration", function() { expect(res.body.position).to.equal(2); expect(res.body.estimatedWaitMin).to.equal(10); expect(res.body.queueStatus).to.equal("WAITING"); });
        test("does not expose access tokens", function() { expect(JSON.stringify(res.body ?? {}).toLowerCase()).to.not.include("accesstoken"); });

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: false
