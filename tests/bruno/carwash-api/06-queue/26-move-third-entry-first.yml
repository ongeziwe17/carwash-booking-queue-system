info:
  name: Move third waiting entry to workflow start
  type: http
  seq: 26
  tags:
    - workflow
    - integrity

http:
  method: PUT
  url: "{{baseUrl}}/api/queue-entries/{{queueOrderingEntry3Id}}/position"
  headers:
    - name: Content-Type
      value: "application/json"
  body:
    type: json
    data: |-
      {
        "position": {{queueOrderingStartPosition}}
      }
  auth:
    type: bearer
    token: "{{staffToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() { expect(res.status).to.equal(200); });
        test("moved entry leads the workflow block", function() { expect(res.body.position).to.equal(Number(bru.getVar("queueOrderingStartPosition"))); expect(res.body.estimatedWaitMin).to.equal(Number(bru.getVar("queueOrderingBaselineWait"))); expect(res.body.queueStatus).to.equal("WAITING"); });
        test("does not expose access tokens", function() { expect(JSON.stringify(res.body ?? {}).toLowerCase()).to.not.include("accesstoken"); });

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: false
