info:
  name: Call next skips in-progress entry
  type: http
  seq: 33
  tags:
    - workflow
    - integrity

http:
  method: POST
  url: "{{baseUrl}}/api/queue-entries/call-next"
  auth:
    type: bearer
    token: "{{staffToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() { expect(res.status).to.equal(200); });
        test("in-progress predecessor is skipped and next waiting entry is called", function() {
          expect(res.body.queueEntryId).to.equal(bru.getVar("queueOrderingEntry2Id"));
          expect(res.body.queueStatus).to.equal("CALLED");
          expect(res.body.booking.status).to.equal("CONFIRMED");
          expect(res.body.position).to.equal(Number(bru.getVar("queueOrderingStartPosition")) + 1);
          expect(res.body.estimatedWaitMin).to.equal(Number(bru.getVar("queueOrderingBaselineWait")) + 15);
        });
        test("does not expose access tokens", function() { expect(JSON.stringify(res.body ?? {}).toLowerCase()).to.not.include("accesstoken"); });

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: false
