info:
  name: Customer A cannot reschedule Customer B booking
  type: http
  seq: 18
  tags:
    - ownership
    - security
    - customer

http:
  method: POST
  url: "{{baseUrl}}/api/bookings/{{otherBookingId}}/reschedule"
  headers:
    - name: Content-Type
      value: "application/json"
  body:
    type: json
    data: |-
      {
        "scheduledDateTime": "{{future33}}"
      }
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns safe HTTP 404", function() { expect(res.status).to.equal(404); });
        test("uses RESOURCE_NOT_FOUND", function() { expect(res.body.code).to.equal("RESOURCE_NOT_FOUND"); });

settings:
  encodeUrl: true
  timeout: 15000
  followRedirects: false
