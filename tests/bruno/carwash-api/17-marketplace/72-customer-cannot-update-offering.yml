info:
  name: Customer cannot update offering
  type: http
  seq: 72
  tags: [marketplace, offering, customer, rbac, security]

http:
  method: PUT
  url: "{{baseUrl}}/api/marketplace/offerings/{{marketplaceOfferingId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {"price":10.00,"estimatedDurationMin":10,"concurrentCapacity":1}
  auth:
    type: bearer
    token: "{{customerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 403", function() { expect(res.status).to.equal(403); expect(res.body.code).to.equal("ACCESS_DENIED"); });

settings: { encodeUrl: true, timeout: 15000, followRedirects: false }

