info:
  name: Unauthenticated offering activation is rejected
  type: http
  seq: 89
  tags: [marketplace, offering, auth, security]

http:
  method: POST
  url: "{{baseUrl}}/api/marketplace/offerings/{{marketplaceOfferingId}}/activate"
  auth:
    type: none

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 401", function() { expect(res.status).to.equal(401); expect(res.body.code).to.equal("AUTHENTICATION_REQUIRED"); });

settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
