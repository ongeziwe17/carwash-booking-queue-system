info:
  name: Invalid offering price is rejected
  type: http
  seq: 50
  tags: [marketplace, offering, validation]

http:
  method: POST
  url: "{{baseUrl}}/api/marketplace/branches/{{marketplaceBranchId}}/offerings"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "offeringId": "{{marketplaceInvalidOfferingId}}",
        "serviceId": "{{serviceId}}",
        "price": -0.01,
        "estimatedDurationMin": 30,
        "concurrentCapacity": 2
      }
  auth:
    type: bearer
    token: "{{ownerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 400 validation error", function() { expect(res.status).to.equal(400); expect(res.body.code).to.equal("VALIDATION_FAILED"); });
        test("identifies price", function() { expect(JSON.stringify(res.body.fieldErrors)).to.include("price"); });

settings: { encodeUrl: true, timeout: 15000, followRedirects: false }

