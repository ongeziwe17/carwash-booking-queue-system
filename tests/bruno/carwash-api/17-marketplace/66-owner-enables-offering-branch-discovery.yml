info:
  name: Business owner enables offering branch discovery
  type: http
  seq: 66
  tags: [marketplace, offering, owner, workflow]

http:
  method: PUT
  url: "{{baseUrl}}/api/marketplace/branches/{{marketplaceBranchId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "branchName": "Updated Bruno Branch",
        "addressLine1": "2 Dock Road",
        "addressLine2": "Unit 5",
        "city": "Cape Town",
        "province": "Western Cape",
        "postalCode": "8001",
        "countryCode": "ZA",
        "latitude": -33.908,
        "longitude": 18.42,
        "timezone": "Africa/Johannesburg",
        "publicDiscoveryEnabled": true
      }
  auth:
    type: bearer
    token: "{{ownerToken}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() { expect(res.status).to.equal(200); });
        test("branch opts back into discovery", function() { expect(res.body.publicDiscoveryEnabled).to.equal(true); expect(res.body.discoverable).to.equal(true); });

settings: { encodeUrl: true, timeout: 15000, followRedirects: false }

