info:
  name: Create booking for active queue rejection
  type: http
  seq: 7
  tags: [workflow, customer]
http:
  method: POST
  url: "{{baseUrl}}/api/bookings"
  headers: [{ name: Content-Type, value: "application/json" }]
  body:
    type: json
    data: |-
      {
        "bookingId": "{{rescheduleQueuedBookingId}}",
        "userId": "{{customerUserId}}",
        "vehicleId": "{{customerVehicleId}}",
        "branchId": "{{opsBranchId}}",
        "serviceOfferingId": "{{opsOfferingId}}",
        "scheduledDateTime": "{{future38}}",
        "specialRequest": "reschedule queue rejection"
      }
  auth: { type: bearer, token: "{{customerToken}}" }
runtime:
  scripts:
    - type: tests
      code: test("returns 201", function() { expect(res.status).to.equal(201); });
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
