info:
  name: Reschedule rejects cancelled booking
  type: http
  seq: 12
  tags: [workflow, customer, integrity]
http:
  method: POST
  url: "{{baseUrl}}/api/bookings/{{rescheduleQueuedBookingId}}/reschedule"
  headers: [{ name: Content-Type, value: "application/json" }]
  body:
    type: json
    data: '{ "scheduledDateTime": "{{future40}}" }'
  auth: { type: bearer, token: "{{customerToken}}" }
runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 400", function() { expect(res.status).to.equal(400); });
        test("reports current state", function() { expect(res.body.message).to.equal("Booking cannot be rescheduled in its current state"); });
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
