info:
  name: Confirm lifecycle reschedule booking
  type: http
  seq: 19
  tags: [workflow, staff]
http:
  method: POST
  url: "{{baseUrl}}/api/bookings/{{rescheduleLifecycleBookingId}}/confirm"
  auth: { type: bearer, token: "{{staffToken}}" }
runtime:
  scripts:
    - type: tests
      code: test("returns confirmed booking", function() { expect(res.status).to.equal(200); expect(res.body.status).to.equal("CONFIRMED"); });
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
