info: { name: Missing longitude is rejected, type: http, seq: 11, tags: [discovery, validation] }
http:
  method: GET
  url: "{{baseUrl}}/api/marketplace/branches/discoverable/nearby?latitude=-33.9249"
  auth: { type: bearer, token: "{{customerToken}}" }
runtime:
  scripts:
    - type: tests
      code: 'test("returns standard 400", function() { expect(res.status).to.equal(400); expect(res.body.code).to.equal("MISSING_PARAMETER"); });'
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
