info: { name: Invalid radius is rejected, type: http, seq: 13, tags: [discovery, validation, radius] }
http:
  method: GET
  url: "{{baseUrl}}/api/marketplace/branches/discoverable/nearby?latitude=-33&longitude=18&radiusKm=0"
  auth: { type: bearer, token: "{{customerToken}}" }
runtime:
  scripts:
    - type: tests
      code: 'test("returns standard 400", function() { expect(res.status).to.equal(400); expect(res.body.code).to.equal("VALIDATION_FAILED"); });'
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
