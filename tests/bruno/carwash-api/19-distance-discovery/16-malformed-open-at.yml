info: { name: Malformed open instant is rejected, type: http, seq: 16, tags: [discovery, validation, schedule] }
http:
  method: GET
  url: "{{baseUrl}}/api/marketplace/branches/discoverable/nearby?latitude=-33&longitude=18&openAt=not-an-instant"
  auth: { type: bearer, token: "{{customerToken}}" }
runtime:
  scripts:
    - type: tests
      code: 'test("returns standard 400", function() { expect(res.status).to.equal(400); expect(res.body.code).to.equal("INVALID_PARAMETER"); });'
settings: { encodeUrl: true, timeout: 15000, followRedirects: false }
